apiVersion: v1
kind: Service
metadata:
  name: my-clusterip-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80         # Service port
      targetPort: 8080 # Pod container port
  type: ClusterIP
